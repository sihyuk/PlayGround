<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidplayground">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RetrofitActivity"></activity>
        <activity android:name=".SqlDatabase" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OnBoardingActivity" />
        <activity android:name=".RecyclerViewExample" />
        <activity android:name=".FragmentCommunication" />
        <activity
            android:name=".FragmentOnRuntimeActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FragmentExample"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FinalActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DisplayActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>